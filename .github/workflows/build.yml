name: Build ImmortalWrt 24.10.6 x86_64

on:
  workflow_dispatch:
    inputs:
      rootfs_size:
        description: 'Rootfs size (MB)'
        required: true
        default: '4096'
      include_docker:
        description: 'Include Docker'
        required: true
        default: 'yes'
        type: choice
        options:
          - 'yes'
          - 'no'

permissions:
  contents: write
  actions: write

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_BRANCH: v24.10.6
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: .config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai
jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Show initial disk usage
      run: |
        echo "=== 初始磁盘状态 ==="
        df -hT
        echo "====================="

    # 清理磁盘空间（确定性脚本，不依赖第三方 action）
    - name: Free disk space
      run: |
        echo "=== 释放磁盘空间 ==="
        sudo rm -rf /opt/hostedtoolcache /usr/share/dotnet /usr/local/lib/android /usr/local/share/powershell /usr/local/.ghcup /usr/local/julia* /opt/ghc
        sudo docker image prune --all --force 2>/dev/null || true
        sudo swapoff -a 2>/dev/null || true
        sudo dd if=/dev/zero of=/swapfile bs=1M count=4096 status=none 2>/dev/null
        sudo mkswap /swapfile 2>/dev/null
        sudo swapon /swapfile 2>/dev/null
        df -hT /

    - name: Show disk usage after cleanup
      run: |
        echo "=== 清理后磁盘状态 ==="
        df -hT
        echo "======================="

    - name: Install build dependencies
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        export TMPDIR=$RUNNER_TEMP
        sudo mkdir -p $TMPDIR
        sudo apt-get clean
        sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/*
        timeout 120 sudo apt-get update -y || {
          echo "apt-get update timeout, retrying..."
          timeout 120 sudo apt-get update -y
        }
        timeout 300 sudo apt-get install -y --no-install-recommends \
          ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache clang cmake cpio curl device-tree-compiler e2fsprogs fastjar flex gawk gettext \
          gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 \
          libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev \
          libncurses-dev libpython3-dev libreadline-dev libssl-dev libtool libyaml-dev libz-dev \
          lld llvm lrzsz mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf \
          python3 python3-pip python3-pyelftools python3-setuptools qemu-utils re2c rsync \
          scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto \
          xxd zlib1g-dev zstd
        sudo apt-get clean
        sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/*
        df -hT / /workdir 2>/dev/null || df -hT /
        echo "--- Build dependencies installed ---"

    - name: Setup build environment
      run: |
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: Checkout
      uses: actions/checkout@v6

    - name: Clone source code
      working-directory: /workdir
      run: |
        df -hT $PWD
        timeout 300 git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: Restore dl cache
      uses: actions/cache/restore@v5
      with:
        path: /workdir/openwrt/dl
        key: dl-${{ env.REPO_BRANCH }}

    - name: Restore ccache
      uses: actions/cache/restore@v5
      with:
        path: ~/.ccache
        key: ccache-${{ env.REPO_BRANCH }}-${{ hashFiles('.config') }}
        restore-keys: |
          ccache-${{ env.REPO_BRANCH }}-
          ccache-

    - name: Load custom feeds
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf.default
        chmod +x $DIY_P1_SH
        cd openwrt
        timeout 60 $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: Update feeds
      run: timeout 600 bash -c 'cd openwrt && ./scripts/feeds update -a'

    - name: Install feeds
      run: timeout 600 bash -c 'cd openwrt && ./scripts/feeds install -a'

    - name: Load custom configuration
      env:
        INCLUDE_DOCKER: ${{ github.event.inputs.include_docker }}
        ROOTFS_SIZE: ${{ github.event.inputs.rootfs_size }}
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH
        cd openwrt
        timeout 600 $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: Save runtime tarballs
      working-directory: openwrt
      run: |
        if [ -f files/lib/ld-musl-i386.so.1 ] || [ -f files/lib/ld-linux.so.2 ]; then
          mkdir -p $GITHUB_WORKSPACE/runtime
          $GITHUB_WORKSPACE/scripts/build-runtime-tarballs.sh
          echo "=== 运行时 tarball ==="
          ls -lh $GITHUB_WORKSPACE/runtime/ 2>/dev/null || true
        else
          echo "(no 32-bit runtime extracted, skipping tarball)"
        fi

    - name: Upload runtime tarballs (artifacts)
      uses: actions/upload-artifact@v7
      if: '!cancelled()'
      with:
        name: runtime-tarballs
        path: runtime/
        retention-days: 7
        if-no-files-found: ignore

    - name: Download all sources
      working-directory: openwrt
      run: |
        echo "=== 下载前磁盘状态 ==="
        df -hT .
        echo "======================="
        timeout 1800 make download -j$(nproc) V=s
        echo "=== 下载后磁盘状态 ==="
        df -hT .
        echo "======================="

    - name: Compile the firmware
      id: compile
      working-directory: openwrt
      run: |
        echo "=== 编译前磁盘状态 ==="
        df -hT .
        echo "======================="
        timeout 120 make defconfig
        echo "$(nproc) thread compile"
        if ! timeout 14400 make -j$(nproc) V=s; then
          echo "Parallel build failed, retrying with single thread..."
          timeout 28800 make -j1 V=s
        fi
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET_DEVICE_.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: Check space usage
      if: '!cancelled()'
      run: df -hT

    - name: Save ccache
      continue-on-error: true
      uses: actions/cache/save@v5
      if: '!cancelled()'
      with:
        path: ~/.ccache
        key: ccache-${{ env.REPO_BRANCH }}-${{ hashFiles('.config') }}

    - name: Organize files
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && success()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware to release
      uses: softprops/action-gh-release@437e073e786973c6b6af97d9e445c41ae43b1d29 # master (node24)
      if: env.UPLOAD_RELEASE == 'true' && success()
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: immortalwrt-24.10.6-${{ github.run_number }}
        body_path: release.txt
        files: ${{ env.FIRMWARE }}/*squashfs-combined-efi.img.gz

    - name: Remove old Releases
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        timeout 120 bash << 'SCRIPT'
          KEEP=3
          gh release list --limit 100 --json tagName --jq '.[].tagName' \
            | tail -n +$((KEEP + 1)) \
            | while read tag; do
                [ -n "$tag" ] && gh release delete "$tag" --cleanup-tag --yes 2>/dev/null || true
              done
        SCRIPT

    - name: Delete workflow runs
      uses: Mattraks/delete-workflow-runs@v2
      with:
        retain_days: 0
        keep_minimum_runs: 2
